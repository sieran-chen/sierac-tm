# ─── Cursor Admin API（必填，否则用量/支出/成员均为空）──────────────────────────
# 获取：cursor.com/dashboard → Settings → Advanced → Admin API Keys
# 详见项目 docs/CURSOR-API-SETUP.md
CURSOR_API_TOKEN=your_cursor_admin_api_token_here
CURSOR_API_URL=https://api.cursor.com

# ─── 数据库 ────────────────────────────────────────────────────────────────────
POSTGRES_USER=cursor
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_DB=cursor_admin
DATABASE_URL=postgresql+asyncpg://cursor:change_me_strong_password@db:5432/cursor_admin

# ─── 同步间隔 ──────────────────────────────────────────────────────────────────
SYNC_INTERVAL_MINUTES=60

# ─── 内部 API 密钥（管理端 ↔ 采集服务通信）────────────────────────────────────
INTERNAL_API_KEY=change_me_internal_key

# ─── 邮件告警（SMTP，可选）────────────────────────────────────────────────────
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=alert@example.com
SMTP_PASSWORD=smtp_password
SMTP_FROM=alert@example.com
SMTP_USE_SSL=true

# ─── 默认 Webhook（企业微信/钉钉，可选）──────────────────────────────────────
DEFAULT_WEBHOOK_URL=

# ─── GitLab（立项时自动创建仓库并注入 Hook，可选）────────────────────────────
# 不配置则仅支持手动填写 git_repos，不调用 GitLab API
# GITLAB_URL=https://gitlab.company.com
# GITLAB_TOKEN=glpat-xxxxxxxxxxxx
# GITLAB_GROUP_ID=123
# GITLAB_DEFAULT_BRANCH=main
# GITLAB_VISIBILITY=private

# ─── Git 采集（定时扫描项目仓库写入 git_contributions，可选）──────────────────
# GIT_REPOS_ROOT=/data/git-repos
# GIT_COLLECT_DAYS=3

# ─── 管理端 ────────────────────────────────────────────────────────────────────
VITE_API_KEY=change_me_internal_key

# ─── 构建（可选，国内服务器 pip 超时时可设）──────────────────────────────────────
# PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
